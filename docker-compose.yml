version: "3"

services:
  app:
    build:
        context: .
        dockerfile: Dockerfile
    image: polybot
    volumes:
      - ./data/database.db:/app/data/database.db:Z
      - ./data/slash.json:/app/data/slash.json:Z
